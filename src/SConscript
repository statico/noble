# -*- mode: python -*-
Import('env')

env.Prepend(CCFLAGS = '-g -Wall -m32')
env.Prepend(LINKFLAGS = '-m32')
env.Append(CPPPATH = Dir('../deps/v8/src'))
env.Append(LIBS = File('../deps/v8/libv8_g.a'))

env.Program(target='noble', source=['noble.cc'])
